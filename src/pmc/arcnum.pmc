pmclass ArcNum 
  extends ArcT
  extends Float
  dynpmc
  group primitivearc_group
  hll Arc
  maps Float {

	VTABLE FLOATVAL get_number() {
		return Parrot_Float_get_number(INTERP, SELF);
	}

	METHOD pr_repr() {
		const FLOATVAL n = SELF.get_number();
		STRING *res = Parrot_sprintf_c(INTERP, "%f", n);
		RETURN(STRING *res);
	}

	METHOD to_string() {
		const FLOATVAL n = SELF.get_number();
		STRING *res = Parrot_sprintf_c(INTERP, "%f", n);
		RETURN(STRING *res);
	}

	VTABLE STRING* name() {
		return Parrot_str_new_constant(INTERP, "num");
	}

}
